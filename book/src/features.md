# Features

Slate promises full C23 support. (We're not there yet). That means
support for attributes (`[[nodiscard]]`), `nullptr`, `alignas`,
`alignof`, `static_assert`, `thread_local`, `constexpr`, `_Decimal32`,
