# Verify sys/sendfile.h, sys/shm.h, and sys/signal.h

_2026-09-17 17:05:28_

All three present on every glibc/musl x86_64/i386/arm/aarch64 descriptor. sys/sendfile.h: real glibc pulls in sys/types.h for off_t/ssize_t while musl pulls in unistd.h, so gated the include by __SLATE_LIBC_GLIBC instead of always using unistd.h. sys/shm.h: added the missing SHM_HUGE_16KB macro, and replaced the shared unconditional 'struct shminfo { ...; __unused[4]; }' in bits/shm.h's generic branch (x86_64/aarch64) plus bits/x86/shm.h (i386) and bits/arm/shm.h (arm) with a glibc-gated variant using four named __glibc_reserved1..4 fields, matching real glibc layout; musl keeps the __unused[4] array. sys/signal.h was already a correct redirect to <signal.h> and needed no changes. All eight focused declaration matrices pass; full libc profile has only the two pre-existing __SlateVaArgs va_args failures (regression + functional suites), unchanged by this work.
