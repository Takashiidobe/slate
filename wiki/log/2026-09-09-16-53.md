# Batch Linux header matrices and parallelize reconciliation

_2026-09-09 16:53:40_

Added memory.h, mntent.h, monetary.h, mqueue.h, and net/ethernet.h to all supported Linux glibc/musl manifests. Reconciled glibc-only mqueue include closure and libc-specific net/ethernet include ownership. Refactored reconcile_header.py to discover oracles and run descriptor fixture/matrix jobs in parallel while retaining deterministic serial manifest and shim writes; --jobs=N controls concurrency. Batched five-header matrix reconciliation passes across all eight descriptors. Full libc, lowering, and rewrites profiles pass. No FileCheck regeneration was needed.
