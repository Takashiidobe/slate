# Fix target libc sched_param layouts

_2026-09-08 07:44:08_

Gated sched_param on __SLATE_LIBC_GLIBC: glibc exposes only sched_priority, while musl retains its reserved fields. Verified struct_sched_param against all four glibc and all four musl targets.
