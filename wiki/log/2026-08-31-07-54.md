# Guard every libc-shim bits header

_2026-08-31 07:54:46_

Added an _SLATE_LIBC direct-inclusion guard to every header under libc-shim/bits, including nested architecture and platform implementation headers and empty placeholders. Existing specific include guidance remains unchanged; newly guarded internal leaves direct users toward public headers. Added a libc_shim_suite filesystem inventory test so new bits headers must place the guard and header-specific diagnostic in their first nine lines. The full release libc nextest profile passes all 25 selected tests, and cargo fmt plus Clippy pass.
