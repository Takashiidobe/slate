# Fix libc-shim corpus header blockers

_2026-08-30 19:43:51_

Fixed five libc-shim blockers found during c-corpus triage. ctype.h now establishes feature context before bits/types.h. pthread_once_t matches scalar Linux initializers while preserving Darwin's long representation. __packed now consistently uses the postfix-compatible GNU attribute spelling in every GCC/Clang C mode. sys/types.h exposes GNU/BSD u_* aliases, and glibc GNU/BSD unistd.h exposes closefrom. Lua now transpiles; PCRE2, libuv, nginx, and QuickJS pass their original diagnostics and expose separately tracked later failures. The libc nextest profile passes 19 tests with 4 skipped. Also removed three redundant borrows reported by Clippy in the existing cross-TU harness; no tests were added.
