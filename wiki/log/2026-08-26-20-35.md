# Fold C23 fixtures into c23_language.c

_2026-08-26 20:35:02_

Deleted the two standalone fixtures (c23_generic_type_operand.c, c23_va_start_single_arg.c) and folded their coverage into the existing c23_language.c fixture instead: added a C23_TYPE_KIND(T) macro exercising _Generic with a type-name operand, wired into language_total. The single-arg va_start form was already exercised there via c23_relaxed_variadic, so no change was needed for that half.

Also dropped the WG14-reference parenthetical annotations from the two features.md entries -- other [x] entries in the doc don't carry that kind of comment, only [~] entries do per the doc's own header convention.

Full lowering profile stays green (10/10).
