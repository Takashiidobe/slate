# Resolve pointer-producing SSA values by CIR type

_2026-08-31 13:12:43_

Implemented slate-hos2.2. Added typed_operand_expr to distinguish ordinary values, function pointers, and data-pointer-producing places. Return lowering now uses the recorded CIR operand type, and call argument lowering shares the same resolver. Added targeted lowering/rewrite coverage for returning a mutable global address, a global pointer address, and an indexed global-array element. All six implicated gcc-torture cases pass individually. The full supported batch now reports only slate-hos2.3 va_list failures and slate-hos2.4 computed-goto aborts.
