# Resolve promoted direct function values before indirect calls

_2026-08-31 13:33:26_

Implemented slate-hos2.7. Indirect call lowering now obtains its callee through function_pointer_operand_expr, so a promoted known function symbol becomes Some(function) before the existing unwrap invocation. Converted unused_params.c from a full lowering snapshot and handwritten rewrite assertion to targeted lowering/rewrite markers around the function-pointer call. Focused lowering and rewrites pass; supported chibicc and c-testsuite lowering batches pass; strict clippy passes.
