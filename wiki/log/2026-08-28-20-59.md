# ptr_len signature worklist with canonical forwarding

_2026-08-28 20:59:21_

Implemented slate-y0qs.4.8.1 in interproc/length_lattice.rs.

Locally bounded pointer/length uses seed candidates. Function-local value definitions canonicalize CIR renames; call sites are indexed once as exact malloc/count roots or forwarding dependencies. The candidate queue is monotone active-to-rejected and visits only dependents, never AST bodies.

Accepted closures rewrite atomically to &[T]/&mut [T], remove the companion length parameter, preserve hoisted argument evaluation, and bridge exact allocations with from_raw_parts[_mut]. Mutable internal views now use from_raw_parts_mut.

Added tests/fixtures/ptr_len_worklist.c covering malloc seed -> forwarding hop -> mutable indexed terminal. Rewrites profile: 110/110. cargo fmt and clippy --release --all-targets clean.

libexpat 21-TU translate-project --lib timings: 5.537s, 5.273s, 5.439s, within the 5.6s target.
