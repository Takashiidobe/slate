# Validate next Linux libc header batch

_2026-09-10 14:46:16_

Validated protocols/rwhod.h, protocols/talkd.h, protocols/timed.h, pthread.h, and pty.h against the configured Linux libc oracles. Added pthread ABI and libc-specific macro handling; re_comp.h has no configured oracle and remains open.
