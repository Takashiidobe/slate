# preserve plain char in long-double shim tags

_2026-09-01 07:28:23_

Fixed strtold long-double bridge generation by adding a dedicated c type tag for CLibType::CHAR. The names-only C shim renderer now reconstructs plain C char recursively, so char ** endptr stays char ** instead of collapsing through x to void **; the tag also preserves target-dependent plain-char signedness rather than conflating it with i8. Modernized long_double_strtold_f80.c to paired region checks. The generated bridge is __slate_strtold__rf80_pc_ppc(char *, char **). Focused lowering and rewrites differential tests pass; strict Clippy/fmt passed before the final tag spelling refinement, and the chibicc supported suite passed. Full profiles were interrupted per user direction. Cross-TU project_strtold expectation regeneration is deferred to a follow-up bead.
