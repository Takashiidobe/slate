# Triage test-fixtures GitHub probes

_2026-08-30 16:53:35_

Triaged the four unclassified GitHub-derived probes in test-fixtures/:
- bitint_record_layout.c: real Slate bug. Static initializer for a _BitInt(65) field nested inside an anonymous inner struct emits a raw integer literal instead of BInt::from(...), so the generated Rust batch fails E0308 (expected BInt<65,2,16>, found integer). Filed slate-m7x7.
- zero_width_bitfield_abi.c: not a Slate bug. clang -emit-cir itself rejects the aligned(16) zero-width-bitfield-tail struct passed by value ('x86_64 calling-convention lowering not yet implemented'), so Slate never sees CIR for it.
- transparent_union_call.c and compound_literal_address.c: both compile and match native C exactly.

Updated test-fixtures/README.md's Result column accordingly.
