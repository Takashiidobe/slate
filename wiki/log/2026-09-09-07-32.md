# Verify five more Linux libc headers

_2026-09-09 07:32:01_

Added oracle-derived declaration matrices for arpa/nameser_compat.h, arpa/telnet.h, arpa/tftp.h, and byteswap.h across glibc and musl on x86_64, i386, ARM, and AArch64; assert.h was already covered and was reverified. Split legacy nameser compatibility exposure by libc, represented TELNET commands as macros, and matched glibc's nested TFTP layout using __packed while retaining musl's layout. Updated matrix collection so directly selected compatibility headers remain visible without treating the selected include as a transitive dependency. Restored existing argz.h typedef and ENOTSUP assertions after fixture generation. The focused declaration matrix, full libc profile, cargo fmt --check, and clippy all pass.
