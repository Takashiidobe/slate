# gnu libc epic: 5.5/5.6/5.7 already satisfied, stdio promoted

_2026-09-04 07:52:12_

Checked the remaining slate-3f8g.5 children against current output.

Method: built every tests/fixtures.generated/gnu_libc_*.rs in a scratch crate with the harness's real deps (libc, vendor/aligned, vendor/bitint, vendor/num-complex, bitfields, edition 2024) and read rustc's warnings directly, rather than trusting the differential pass alone.

- 5.5 (regex/glob/dlfcn closures): satisfied. The intact tests/fixtures/gnu_libc_platform.c passes both profiles. Dl_info, glob_t, and re_pattern_buffer all emit with full field lists (re_pattern_buffer carries __opaque/__padding from the shim). No undeclared re_dfa_t, dirent/stat, or link_map.
- 5.6 (C ABI for callbacks): satisfied. Every fn-pointer in the gnu_libc_* output is `Option<unsafe extern "C" fn(..)>` (15 across allocation/platform/algorithms); grep finds no Rust-ABI fn pointers. rustc emits no improper_ctypes on any callback param or callback-bearing aggregate.
- 5.7 (portable runtime symbol signatures): satisfied. strlen now emits `fn strlen(_0: *const i8) -> usize`, not `*mut i8 -> u64`. Verified by stripping `suspicious_runtime_symbol_definitions` from the generated allow-list and rebuilding: zero warnings of that lint across all five fixtures.

Also promoted tests/fixtures.unsupported/gnu_libc_stdio.c to tests/fixtures/. It was blocked on slate-2o6o.18 (llvm.byval dropped on non-variadic extern struct params, corrupting fopencookie); that landed today, so the fixture now translates, compiles, and matches C exactly (`9 8 6 1`, exit 0). Whole-file FileCheck blocks generated with tools/update_filecheck.py; lowering and rewrites both pass. It overlaps but does not duplicate the narrower tests/fixtures/gnu_cookie_stdio.c.

One real gap found and filed as slate-3f8g.5.10: forward-declared-only structs lower to an empty `#[repr(C)] struct X {}`, which is not FFI-safe. `struct __tab` (glibc's opaque `struct _ENTRY`) triggers three improper_ctypes warnings on hcreate_r/hsearch_r/hdestroy_r. Fix shape is the bindgen opaque idiom `{ _unused: [u8; 0] }`.
