# Generate and run cross-TU FileCheck fixtures in parallel

_2026-08-30 12:33:04_

Closed slate-m53d.

Added project-aware modes to tools/update_filecheck.py. --project and --library-project copy and instrument every annotated C source together, translate the whole fixture once per required profile, locate each marker-bearing generated module, and refresh the owning source blocks. Exported project marker functions and nested marker attributes are excluded from captured regions.

Auto-discovered cross-TU and library FileCheck fixtures now use bounded parallel_map execution. Removed the process-global CARGO_TARGET_DIR mutation; Cargo commands already receive isolated --target-dir paths. FileCheck, Rust compilation, and differential execution reuse the same translated crate.

Migrated 20 ordinary cross-TU differential wrappers plus project_strtold into fixture-owned lowering/rewrites checks and removed their dedicated Rust tests. Kept custom artifact, diagnostic, external-link, ignored, symbol, and section tests dedicated. Added a library-project updater fixture and documented both modes and artifact ownership.

Validation: updater py_compile and idempotent regeneration; cargo fmt; cargo clippy; full rewrites 90/90 in 26.81s; full lowering 10/10 in 54.36s.
