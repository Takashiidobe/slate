# promote gnu_builtins_numeric.c, split remaining gnu_builtins fixtures

_2026-09-04 14:39:50_

slate-2o6o.13 covered a 4-file GNU builtins survey (atomic/core/memory/numeric).
Recent __builtin work (54649ca1, 54298a1e) fixed enough that
gnu_builtins_numeric.c now translates and differentially passes; git mv'd it
into tests/fixtures/ with @lowering-fn/@rewrite-fn markers per function
(gnu_builtin_bits, gnu_builtin_reordering, gnu_builtin_overflow,
gnu_builtin_floating), regenerated via update_filecheck.py, verified under
both lowering and rewrites profiles.

atomic/core/memory still fail at -emit-cir itself (ClangIR CIRGen 'Not Yet
Implemented' for specific X86 builtins: legacy __sync_* ops in atomic;
__builtin_is_aligned/__builtin_extract_return_addr/__builtin_frob_return_addr
in core; __builtin_alloca_with_align/__builtin_memcpy_inline in memory) --
upstream gaps, not Slate lowering bugs, same class as slate-2o6o.16's
_BitInt(>128) block.

Split these into slate-2o6o.21/.22/.23 (one per fixture, each BLOCKED with the
specific missing builtins noted) and closed .13.
