# re-enabled rewrite passes, profiled against libexpat

_2026-08-27 09:01:16_

Uncommented apply_with_logger's full pass sequence in src/backend/mod.rs (was stubbed to
just setjmp_recovery while lowering was the focus) plus the now-live
to_fixpoint_program_with_facts/FixpointLimit/late_loop_cleanup/inline_temp_fixpoint_limit
helpers, removed their #[expect(dead_code)] and the module-level #![allow(dead_code)] in
runtime.rs/facts/mod.rs/query/mod.rs. One correctness gotcha: `let mut debug_done = false;`
must be declared BEFORE `macro_rules! step { ... }`, not after -- macro_rules hygiene
resolves free identifiers in the macro body against bindings visible at the macro's
*definition* site, so declaring debug_done after the macro (as a naive uncomment would
place it, matching where `let mut program = input.clone()` sits) fails to compile with
E0425 despite being lexically "in scope" by ordinary block rules.

Builds clean (cargo build --release), only pre-existing unrelated dead-code warnings.
Ran translate-project --lib against libexpat (~/c-corpus/libexpat/expat/build/compile_commands.json,
21 TUs) with SLATE_FIXUP_TIMING=1: 67.7s wall time, fixup steps alone sum to 53.45s (~79%
of total). See slate-y0qs.1 (closed) for the full profiling breakdown -- rule-matching cost
dominates fixpoint rounds (~90%), set_program diffing is only ~9.5%, confirming the
worklist engine (y0qs.3) over touched-item threading (y0qs.2) as the priority.

Not yet verified: whether libexpat's translated output actually compiles/passes
differential tests with fixups back on -- this session only confirms it runs and times,
not correctness. Full rewrites-profile nextest run also not re-run yet.
