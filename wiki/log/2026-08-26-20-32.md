# C23 _Generic type-operand + va_start single-arg fixtures

_2026-08-26 20:32:06_

Added tests/fixtures/c23_generic_type_operand.c and tests/fixtures/c23_va_start_single_arg.c to confirm the two features flagged as unaudited in the previous features.md pass.

Both passed on the first try under the lowering profile with no lowerer changes needed, confirming the hypothesis from slate-4srf.1/.2: clang resolves _Generic's controlling operand (type-name or expression) during sema before CIR is emitted, and lower_va_start (src/frontend/lowerer/calls.rs) never reads a second CIR operand, so C23's single-argument va_start(ap) form was already handled identically to the two-argument form.

Flipped both features.md entries from [ ] to [x] with fixture references. Closed slate-4srf.1 and slate-4srf.2. Full lowering profile (10/10) still green.
