# chibicc unsupported epic: constexpr/param_omit fixed and promoted, 7 more triaged and blocked

_2026-09-03 14:43:42_

Swept the remaining open children of slate-6v25 (chibicc unsupported triage
epic).

Fixed and promoted (git mv to supported/):
- slate-6v25.9 constexpr.c: array-size exprs `(int *)0 + 2` / `(int *)16 - 1`
  are pointer-typed, not integer -- chibicc's constant folder reinterpreted
  the pointer bit-pattern as the array size without a cast. Added explicit
  `(long)` casts; preserves the intended numeric value.
- slate-6v25.19 param_omit.c: its own omitted-name memcpy redeclaration
  (`void *, const void *, unsigned long`) conflicted with the looser
  memcpy prototype in the shared fixtures.chibicc/test.h (`void *, void *,
  long`). Aligned the fixture's own redeclaration with the already-included
  header instead of touching test.h (shared by 30+ other fixtures). The
  redeclared line wasn't load-bearing for the fixture's actual intent
  (testing omitted-parameter-name syntax on add/mul/sub).

Both confirmed via chibicc_unsupported_tests_still_fail's promotion-hint
panic, then promoted; chibicc_supported_tests_match_c stays green.

Blocked (chibicc-only laxity or real GNU/clang divergence, confirmed against
plain gcc where relevant, not slate-fixable by a small edit):
- slate-6v25.10 control.c: `(i=5,j)=6` assigns to a comma-expr result (gcc
  also rejects); computed-goto jumping across sibling GNU statement-expr
  scopes (gcc accepts, clang correctly flags -- confirmed with a minimal
  repro, real scope-safety rule, chibicc doesn't check it, slate has no
  computed-goto lowering anyway).
- slate-6v25.11 float.c: re-confirmed duplicate of slate-pcek.17, marked
  blocked (was open).
- slate-6v25.13/6v25.18/6v25.22 (generic/literal/sizeof): all three fail on
  the same bare-digit-plus-'f' float literal ("100f", "8f", "1f") --
  chibicc-lexer-only laxity, not valid C in any compiler. Cross-linked as
  duplicates.
- slate-6v25.20 pointer.c: chibicc over-decays multi-dim arrays straight to
  the scalar element pointer type, skipping the standard one-dimension-only
  decay rule -- confirmed against the decay rule directly, not clang
  pickiness.
- slate-6v25.21 predefined_macros.c: `__STDC_IEC_559__` genuinely isn't
  predefined by this clang build/version -- toolchain-level, not
  slate-controlled.
- slate-6v25.24 typedef.c: redeclaring a typedef name 't' as a variable 't'
  in the same scope -- confirmed invalid against real gcc too, plus a
  duplicate -Wimplicit-int case (same class as pcek.12/6v25.7/6v25.8).
- slate-6v25.27 variable.c: incompatible-pointer-type errors chibicc accepts
  without a cast, AND (separately) the fixture's ASSERT literals hard-code
  chibicc's own specific stack-slot layout as the expected byte-distance --
  not portable to any other compiler even after fixing the type errors.

Remaining open children under slate-6v25: none (6v25.24 was last open one
now blocked). All still-failing fixtures now have a documented, verified
root cause.
