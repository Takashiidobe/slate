# c-corpus compile_commands.json + justfile transpile tasks

_2026-08-30 13:32:34_

Gave every project in ~/c-corpus a root-level compile_commands.json for translate-project --lib --compile-commands: deduped (jq group_by(.file)) the existing build/-nested DBs for zlib/lz4/zstd/cJSON/libexpat (libexpat's lives at libexpat/expat/ since that's the actual project root); regenerated sqlite's (was stale, pointing at a since-deleted ~/sqlite checkout instead of c-corpus/sqlite); generated fresh via cmake -DCMAKE_EXPORT_COMPILE_COMMANDS=ON for curl/libpng/libuv/mbedtls/pcre2/utf8proc/yyjson (mbedtls needed `git submodule update --init --recursive` for framework/tf-psa-crypto first); via `bear -- make` for lua/quickjs/redis/tinycc; via `./auto/configure && bear -- make` for nginx. Filtered redis's modules/vector-sets/hnsw.c (name-collides with src/hnsw.c) out of its DB.

Added justfile with just transpile-<name> / check-<name> / transpile-all / check-all recipes, transpile-* driven by SLATE_C_CORPUS-overridable compile_commands.json paths, check-* separated out so cargo check doesn't run automatically after every transpile.

Smoke-tested transpile-{chibicc,libyaml,lz4,zlib,zstd,cjson}: all pass quickly. transpile-redis hangs: no per-TU progress output, memory climbs past 6.5GB/270% CPU with nothing written past crate scaffolding. Bisected by TU count -- a 148-file half of redis's 297-TU db finishes in <20s alone, but the full set times out at 25s and recombining both (independently-fast) halves still hangs, so it's a superlinear blowup in some whole-project pass, not one bad file. Filed slate-a28e.11 (P1).

Mistake: my post-smoke-test cleanup `rm -rf`'d transpiled/chibicc and transpiled/libyaml, which were pre-existing crates (not smoke-test artifacts) and are gitignored/unrecoverable via git. User declined recovery (not important). Lesson: diff `just --list`/glob against known-preexisting dirs before bulk-deleting scratch output in a directory that also holds real work.
