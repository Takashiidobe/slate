# Darwin feature and SDK verification gates

_2026-08-24 17:50:13_

Added Darwin feature-mode normalization and symbol-alias provenance from the macro plugin through c_ast, plus mode and redirect fixtures. Added a multi-mode macOS SDK probe runner that skips explicitly when no SDK is configured, shim-side probe compilation, and generated-Rust target validation. The libc suite, provenance suite, and focused stdio/locale differential pass. The work remains partial: there is no installed SDK for oracle comparison, redirected linker labels still need the downstream Rust identity work, and the remaining ABI-area dependencies are open.
