# preserve trusted declarations in ABI shims

_2026-09-01 17:32:04_

Carry a trusted C declaration through direct call bindings into long-double ABI shims. Render that declaration directly in the C bridge instead of reconstructing its prototype from lowered Rust types. Keep Rust-type reconstruction only as the fallback when no trusted declaration exists. Focused long_double_strtold_f80 lowering differential passes; the full lowering profile remains blocked by the unrelated chibicc packed-structure mismatch.
