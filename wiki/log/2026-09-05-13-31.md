# whole-corpus FileCheck regen with default cross-targets

_2026-09-05 13:31:48_

Ran `just regen-filecheck` (both profiles, all 545 fixtures/projects) after
the __v temp-inlining fix and the new default-target justfile plumbing.
502 files changed (+115464/-98694): every fixture's checked-in Rust shape
now reflects restored temp inlining, and most non-platform fixtures gained
COMMON-*/*-X86_64-GNU/*-AARCH64-GNU split blocks for the first time.

50 "skip: leaving block untouched" lines, all graceful no-ops:
- 19 msvc/* LOWERING-MSVC: pre-existing, no MSVC cross toolchain locally;
  these paths already fell back to their own default_targets_for_path
  triple (my _regen-filecheck guard excludes bionic/macos/msvc from the
  generic linux-gnu default), so this predates today's work.
- 15 x86-only fixtures (asm/cpuid/f128/long_double/bitint/simd
  intrinsics) failing LOWERING-AARCH64-GNU: expected, they don't compile
  for aarch64.
- 9 handwritten-block skips (bionic/macos manual REWRITES-* directives):
  by design, untouched.
- 1 tests/fixtures.multi/keyword_project_idents "0 modules" skip:
  reproduced identically on the pre-fix tree, unrelated pre-existing quirk.
- 7 tests/fixtures.cfg/*.c (arch_targets, arm_endian_targets, ndebug,
  os_targets, pointer_width_targets, unix_target, win64_target)
  REWRITES-X86_64-GNU: newly discovered latent bug in
  tools/update_filecheck.py's fn-scope name scan - `@rewrite-fn-begin`
  immediately followed by `#if defined(...)` makes it read "defined" as
  the wrapped function's name instead of skipping the conditional, so
  extract_generated_regions raises "no definition of fn defined". Never
  fired before since these fixtures never had --target regen attempted.
  Left untouched (no corruption); reported to user as a follow-up, not
  fixed in this pass.
