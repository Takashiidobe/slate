# Persist libc static declaration fixtures

_2026-09-08 16:36:14_

Moved the glibc x86_64 matrix manifest and generated composite probe sources into tests/fixtures.libc-static-test. An ignored generator refreshes fixture sources from the oracle while the ordinary libc matrix compiles only manifest-selected fixtures. The initial green manifest contains strings.h; adding a header makes the normal gate fail until its source exists and libc-shim satisfies it. Matrix selection now excludes underscore-prefixed implementation declarations.
