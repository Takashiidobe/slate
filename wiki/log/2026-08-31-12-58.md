# CIR mem2reg prototype and initial lowering-gap triage

_2026-08-31 12:58:12_

Enabled cir-opt --mem2reg before normal and CFG-flattened CIR ingestion under slate-hos2. Focused add lowering and rewrites differential tests pass and now assert only the marked function shape. The full release lowering profile reaches real gcc-torture Rust compile failures, not merely stale FileCheck snapshots. Initial root causes are tracked as slate-hos2.1 (folded constant globals loaded as values), slate-hos2.2 (pointer-producing SSA operands consumed without address semantics), and slate-hos2.3 (direct va_list SSA operands disagree with the selected Rust wrapper representation). The active prototype task slate-8tme depends on all three. Full run ID: f6a909b9-b7f5-4ff1-afae-07d7485c009f.
