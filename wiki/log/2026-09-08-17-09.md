# Route assert_perror through existing assert failure path

_2026-09-08 17:09:10_

Removed the unnecessary string.h dependency from libc-shim assert.h. GNU glibc assert_perror now treats zero as a no-op and routes nonzero errors through the existing __assert_fail implementation path with a fixed assertion message. Focused macro expansion, declaration matrix, full libc profile (45 passed, 13 skipped), clippy, and formatting pass.
