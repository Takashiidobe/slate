# preserve strlen rewrite result types

_2026-09-01 17:51:38_

Make the StrLen rewrite retain the enclosing lowered let type. A rewritten __builtin_strlen now emits .len() as u64 when the C-lowered value is unsigned long/u64, while usize bindings remain uncast and untyped value contexts are left untouched. Keep known libc bridge prototypes suppressed; trusted declarations remain available for unknown bridge calls.
