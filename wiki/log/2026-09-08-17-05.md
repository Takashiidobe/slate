# Implement GNU glibc assert_perror

_2026-09-08 17:05:01_

Added GNU-profile assert_perror to libc-shim assert.h for glibc. The macro preserves zero-error no-op behavior and routes nonzero errors through strerror and the existing __assert_fail path without exposing glibc's private __assert_perror_fail symbol. Restored assert.h matrix verification passes; actual assert_perror call-site compilation, the full libc profile (45 passed, 13 skipped), clippy, and formatting pass.
