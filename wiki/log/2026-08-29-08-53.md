# Fold pointer return aliases through call sites

_2026-08-29 08:53:45_

Completed slate-y0qs.4.3.

The pointer lattice now summarizes functions whose returns all trace to exactly one pointer parameter. The summary follows null-initialized return temporaries and reaches a fixed point through forwarding wrappers; ambiguous multi-parameter returns fail closed.

Known call results receive synthetic pointer facts linked bidirectionally to the returned callee parameter in the SCC worklist. Proven returns no longer set ESCAPE, so caller-side reads and writes select &T or &mut T through direct and forwarded calls.

Plan validation now recognizes conservative local aliases of lifted parameters when checking wrapper call arguments.

Added pointer_return_alias.c covering mutable, const, forwarded, and ambiguous cases. cargo fmt, strict release clippy, the focused differential fixture, and all 110 rewrite-profile tests pass.
