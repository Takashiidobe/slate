# Validate shadow signal spawn stab stdbit headers

_2026-09-10 15:44:52_

Added shadow.h, signal.h, spawn.h, stab.h, stdbit.h, and stdc-predef.h to the Linux declaration manifests where exposed. Matched glibc type-specific stdbit macros, glibc spawn cgroup and legacy signal surfaces, musl signal macros and mcontext naming, and target-specific include closures. The compliance script and full libc profile pass.
