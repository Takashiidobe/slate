# Audit inline-asm x86/ARM/AArch64, refresh stale concept doc

_2026-09-07 13:40:17_

Audited src/frontend/lowerer/asm.rs + intrinsics.rs against
wiki/concepts/inline-asm-support.md and bd (slate-3f8g.4.15,
slate-3f8g.4.16, slate-os0h.3.1.66, slate-2o6o.8). Found the concept doc's
"Todo" section was stale: byte-sized Q/q (slate-3f8g.4.15.10) is
implemented and closed, but the doc still described it as a filed
follow-up with no landed code.

Fixed: added the byte-sized Q/q widen-hack to the storage-class table and
gave it its own subsection (widen to i32 via reg_abcd, {N:l}/{N:h} views,
%h unsupported on byte-sized operands since real Clang never populates the
high byte there). Renamed "Todo" to "Known x86 gaps" and cut it down to
the two constraints that are still genuinely unimplemented (y/MMX — a
permanent rustc limitation; A/edx:eax pair — a new tied-pair lattice shape,
not yet built), pointing at bd for the actively-moving gcc-torture triage
work instead of re-narrating it here.

No remaining doc drift found elsewhere: AArch64/ARM sections already match
current closed/open bd state (Y float-constant gap = slate-3f8g.4.16.18,
still open, correctly described).
