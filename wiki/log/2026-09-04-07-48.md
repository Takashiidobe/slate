# gnu getopt/search type closure already satisfied

_2026-09-04 07:48:30_

slate-3f8g.5.3 was filed because gnu_libc_algorithms.c emitted `struct option` as an unresolved option type and `struct hsearch_data` referred to an unresolved `_ENTRY`.

Verified today: the intact fixture (qsort_r + getopt_long + argp_parse + hcreate_r/hsearch_r/hdestroy_r + tsearch/tfind/tdestroy in one file) now lives in tests/fixtures/gnu_libc_algorithms.c and passes both the lowering and rewrites differential profiles.

Generated output confirms complete type closure:
- `struct option { name, has_arg, flag, val }` full definition, used by `getopt_long(.., *const option, ..)`.
- `struct hsearch_data { __tab: *mut __tab, __unused1, __unused2 }` with `struct __tab {}` as an empty repr(C) struct. Correct, not a gap: libc-shim/include/search.h forward-declares `struct __tab` and only uses it behind a pointer, matching glibc's opaque `struct _ENTRY`.
- `argp`, `argp_state`, `argp_child`, `argp_option`, `entry`, and the `ACTION`/`VISIT` enums all lower with full field lists and C-ABI fn-pointer members.
- `hsearch_r` lowers its by-value ENTRY parameter into two pointer args: the SysV ABI decomposition, not a signature bug.

No code change needed; closed as already fixed by intervening libc-shim and type-closure work.
