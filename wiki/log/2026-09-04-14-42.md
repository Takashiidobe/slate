# promote bitfield fixtures, close slate-2o6o.3

_2026-09-04 14:42:36_

slate-2o6o.3 described bitfield_storage.c / bitfield_init_increment.c /
bitfield_ops.c as blocked on 'no C bitfield support' -- stale.
src/frontend/lowerer/bitfields.rs already covers plain/mixed-signedness
bitfields, union-of-struct-with-bitfields, and 40-bit wide bitfields
(struct Wide in bitfield_ops.c). All three fixtures translated and
differentially passed on first try, no code changes needed.

git mv'd all three into tests/fixtures/ with @lowering/@rewrite region
markers around the interesting statements (bitfield_ops.c got two disjoint
regions: struct Bits ops, struct Wide 40-bit ops), regenerated via
update_filecheck.py, verified under both lowering and rewrites profiles.
Closed slate-2o6o.3.

zero_width_bitfield_abi.c also lives in fixtures.unsupported/ but isn't in
this issue's scope (title only covers storage/init_increment/ops) -- left
untouched, may be worth its own triage pass.
