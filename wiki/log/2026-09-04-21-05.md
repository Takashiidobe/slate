# Compare early CIR against the default Clang pipeline

_2026-09-04 21:05:09_

slate-77g3: inspected pass ordering after LLVM rebuild.
LoweringPrepare moved before CallConvLowering in aacef70b0840f;
complex runtime calls now receive ABI classification, explaining result mismatches.
-disable-llvm-passes does not disable the CIR transformation pipeline.
-Xclang -clangir-disable-passes emits initial CIR; cir-opt can then run
--cir-canonicalize --mem2reg while preserving complex operations and struct signatures.
Compared a struct/loop/complex probe plus bitfields, alloca, x86 intrinsics,
and optimized goto fixtures in /tmp/slate-cir-comparison.
Raw plus canonicalization matched default bitfield fixture output exactly.
Early CIR retains structured loops; mem2reg does not eliminate all loop-carried slots.
-Xclang -fno-clangir-call-conv-lowering is a narrower option that retains
runtime expansion but avoids ABI rewriting of those calls.
No production pipeline change yet; corpus validation remains necessary.
Lifetime start/end lowering and regenerated FileChecks are in the working tree.
Ten fixtures now have paired lowering/rewrite function markers.
Differential profiles have no FileCheck failures after regeneration;
complex compilation failures remain pending pipeline choice.
