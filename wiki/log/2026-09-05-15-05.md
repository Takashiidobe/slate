# Correct slate-yj3o: long_double align(16) was a stale snapshot, not a regression

_2026-09-05 15:05:54_

User corrected my triage: x86_64 System V ABI stores 80-bit long double with
16-byte alignment, so #[repr(C, align(16))] struct LongDouble([u8; 10]) is
correct current codegen. The FileCheck failure was the same root-cause-1
bug (bag-diff commonize_checks breaking CHECK-NEXT adjacency), just in a file
my earlier sweep missed: tests/fixtures.link/long_double/{main,link}.c live
outside tests/fixtures/, so the tests/fixtures/*.c-only grep didn't catch
them. Regenerated both with the interleaved scheme; long_double_x86_64_gnu
now passes under both profiles. Differential failures: 10 -> 9.

Noted but not investigated: tests/fixtures.multi/*.c and tests/fixtures.cfg/*.c
also carry hand-written *-x86_64-gnu/*-aarch64-gnu blocks (update_filecheck.py
--project explicitly disallows --target, so these can't be tool-regenerated
the same way). None currently fail, so left alone -- flag if that changes.
