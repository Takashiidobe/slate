# Fix target libc setjmp and ucontext layouts

_2026-09-08 07:42:39_

Updated setjmp.h with ISA-gated jmp_buf storage sizes and signal headers with glibc-only x86 and x86-64 ucontext tails. Verified jmp_buf, sigjmp_buf, and ucontext_t records match all four musl and all four glibc targets.
