# aarch64 skip-list: add f128_intrinsics

_2026-09-07 11:32:10_

Pre-existing, unrelated to slate-3f8g.4.16.7-9: f128_intrinsics.c fails
under --profile aarch64-lowering/aarch64-rewrites because SLATE_CLANG's
CIR frontend doesn't support __float128 for aarch64-unknown-linux-gnu -
same root cause as the existing "float128"/"long_double" skip-list entries
(slate-sdks.1), just a differently-named fixture that wasn't covered by
that match arm. Added "f128_intrinsics" alongside them in
tests/differential_aarch64.rs::skip_reason. Confirmed pre-existing by
stashing my working changes and reproducing the same failure on main.
Full aarch64-lowering and aarch64-rewrites profiles now pass cleanly.
