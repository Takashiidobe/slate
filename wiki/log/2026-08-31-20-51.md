# rewrite for turning byte strings into Cstrings in rust

_2026-08-31 20:51:13_

turned byte strings into c strings in rust, allowing as to also strip the cast after .as_ptr(). Also added helpers in walk
