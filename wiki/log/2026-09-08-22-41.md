# Centralize glibc transitive header overrides

_2026-09-08 22:41:32_

Moved glibc transitive-header exceptions into the declaration probe extractor. The centralized policy now filters selected compatibility and implementation headers consistently from header visibility, declarations, types, objects, and macros. Removed the matrix's per-header ignore table and added strict visibility comparison before symbol probes. Regenerated the glibc x86 declaration inputs and verified the remaining failures are actual argp/arpa symbol-surface gaps. Preserved musl behavior by making the overrides glibc-only.
