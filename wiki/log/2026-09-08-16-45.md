# Generalize declaration matrix test descriptors

_2026-09-08 16:45:14_

Replaced the hardcoded glibc x86_64 declaration matrix test with descriptor-driven target selection. Descriptors now own architecture, libc, feature profile, manifest, fixture root, and oracle root. Added SLATE_LIBC_DECL_TARGET selection for the ignored generator and verifier, and aggregate failures across enabled descriptors and manifest-selected headers. cargo fmt and focused clippy pass. The libc profile reaches the verifier but is red because the pre-existing assert.h manifest addition generates unsupported __assert declarations and private feature macro checks in libc-shim.
