# Model FreeBSD feature selection and symbol versions (slate-sfzn.10.10)

_2026-08-31 19:28:29_

Implemented the FreeBSD 15.1 sys/_visible.h namespace model with exact __BSD_VISIBLE, __POSIX_VISIBLE, __XSI_VISIBLE, ISO C, and Annex K levels. Default FreeBSD exposes BSD/POSIX/XSI extensions without synthesizing glibc feature macros; strict POSIX, XSI, and ANSI modes narrow declarations. sys/param.h now publishes the pinned __FreeBSD_version=1501000 and the feature profile rejects other release baselines.

Mode fixtures compile on amd64 and arm64 and verify representative positive and negative declaration surfaces. The shim now models historical qsort_r selection through __qsort_r_compat plus .symver qsort_r@FBSD_1.0 while retaining the modern POSIX form.

Extended the Clang macro provenance plugin with a first-pass FileScopeAsmDecl .symver collector. CallSymbolFact now stores source_name, resolved foreign_name, and an optional symbol_version. The actual FreeBSD qsort fixture verifies qsort_r / qsort_r / FBSD_1.0 end to end through c_ast.

libc profile passes 34/34. Normal clippy passes with two warnings from concurrent inline_temps work. The lowering profile's unrelated goto/chibicc failures also track those concurrent edits; all other lowering tests passed.
