# Normalize Intel inline-asm dialect wrappers

_2026-08-30 15:55:31_

Fixed slate-a13q by recognizing extended-asm templates wholly wrapped in .intel_syntax noprefix and .att_syntax prefix. Slate now strips those source-dialect directives, translates %Vn operands under Rust's native Intel dialect, and carries that effective dialect through EBX fixups and final InlineAsm emission. Added tests/fixtures/asm_dialect_switch.c with generated lowering and rewrite assertions for both the Intel wrapper and an ordinary AT&T statement. Native/generated behavior is 12 3. Isolated lowering and rewrites probes pass; the full release lowering profile passes 10/10; cargo fmt and release all-target clippy pass with three pre-existing cross_tu needless-borrow warnings.
