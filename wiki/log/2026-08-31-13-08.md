# Lower mem2reg constant globals as rvalues

_2026-08-31 13:08:23_

Implemented slate-hos2.1. CIR mem2reg turns local aggregate initialization into get_global plus load of folded private constants. lower_load now uses the same typed constant renderer as lower_copy for strings, arrays, aggregates, and zero initializers, while volatile and atomic loads remain place-based. Added targeted lowering/rewrite FileCheck and differential coverage in mem2reg_const_global_load.c. All 19 implicated gcc-torture cases pass individually. The full batch also exposed a separate indirect-goto liveness miscompile, tracked as slate-hos2.4.
