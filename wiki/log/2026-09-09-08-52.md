# Complete fmtmsg fnmatch fpu-control and fstab matrices

_2026-09-09 08:52:00_

Added fmtmsg.h and fnmatch.h to every Linux glibc/musl declaration manifest, with macro-form constants matching the oracle. Added glibc-only fpu_control.h and fstab.h to the four glibc manifests and glibc-only exclusion list; their non-glibc direct includes now reject. fpu_control_t is 16-bit on x86/x86_64 and 32-bit elsewhere; AArch64 alone includes sys/types.h, matching its oracle closure. fstab now supplies the public record, constants, and access API. All declaration matrices and the full release libc profile pass; cargo fmt --check and Clippy with warnings denied pass.
