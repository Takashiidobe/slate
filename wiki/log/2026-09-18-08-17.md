# Auto-discover libc rejection FileCheck fixtures

_2026-09-18 08:17:21_

Reworked slate-khfh.1 after review: rejection fixtures now carry their own C89-compatible compiler flags and FileCheck diagnostic assertions. The suite discovers all .c files in the reject directory and checks compiler failures with FileCheck, so adding a fixture needs no Rust registration. The checks require the intended diagnostic and a single compiler error. The focused suite passed; full libc profile and final gates are being run.
