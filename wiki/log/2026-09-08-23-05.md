# Repair strict Linux libc declaration matrices

_2026-09-08 23:05:39_

Fixed the full libc profile after enabling all glibc/musl declaration descriptors. Removed the duplicate signal union sigval definition now supplied through bits/types.h. Narrowed oracle implementation-header collection to target-owned bits headers so fcntl retains O_RDONLY and struct flock without leaking unrelated transitive signal/socket macros. Classified features.h as configuration-only and the AArch64 signal implementation's sys/procfs.h and sys/user.h as glibc transitive exceptions. Generated the missing i386, ARM, and AArch64 glibc/musl matrix fixtures. Verified all 47 libc-profile tests, cargo fmt, Clippy with warnings denied, and git diff checks.
