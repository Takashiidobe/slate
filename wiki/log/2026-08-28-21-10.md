# ptr_len owned Vec signature lifting

_2026-08-28 21:10:16_

Implemented slate-y0qs.4.8.2 by extending the canonical ptr_len candidate graph to ResolvedPtrType::Vec.

Exact malloc(count*sizeof(T)) roots bridge with Vec::from_raw_parts(ptr,count,count); forwarding candidates move the existing Vec. Matching terminal free calls are removed and replaced by Vec::Drop while hoisted argument temporaries remain.

Added ptr_len_vec_worklist.c for exact allocation -> forwarding -> indexed mutation/free, and ptr_len_vec_capacity_reject.c proving extra-capacity allocations fail closed because their deallocation layout would differ.

Kept Str/StringOwned disabled pending slate-y0qs.4.4's validity fact. Optimized seed discovery so the local free scan runs only for functions with Vec-resolved params.

Validation: rewrites 110/110; isolated positive/negative fixtures green; fmt/clippy clean. libexpat after the fast-path fix: 5.548s, 5.361s, 5.644s (5.518s average).
