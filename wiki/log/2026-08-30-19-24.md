# Full c-corpus compile-command triage

_2026-08-30 19:24:39_

Ran all 21 just transpile-* recipes with a strict 60-second timeout, then cargo-checked every successfully generated crate with the same ceiling.

Green through cargo check: cJSON, libexpat, libyaml, lz4, utf8proc, yyjson, zlib, zstd.

Chibicc translates but fails cargo check because tokenize.rs emits duplicate declarations for 52 long-double helpers (slate-a28e.23).

Translation blockers filed: same-stem module collisions in curl/mbedTLS/musl (.16); ctype.h missing features.h before bits/types.h in Lua/PCRE2 (.17); pthread_once initializer ABI mismatch in libuv (.18); nginx epoll packed placement and missing u_char (.19/.20); QuickJS closefrom omission (.21); TinyCC guarded-warning cfg translation (.22).

Redis and SQLite both hit timeout 124 at 60 seconds and are consolidated as the same paused blowup in existing .11.

Libpng also fails its original host compile command due to a stale generated zlib version/configuration mismatch, so filed corpus setup task .24 rather than a Slate code bug.
