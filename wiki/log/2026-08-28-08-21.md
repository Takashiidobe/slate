# Implement pointer capability lattice interproc solve (slate-y0qs.4.1)

_2026-08-28 08:21:38_

Landed src/backend/interproc/pointer_lattice.rs: PointerFact (the seven
monotone bits from wiki/log/2026-08-28-07-48.md), resolved()/
specialize_string() implementing the core+string tables, and solve() --
per-function local classification (classify_function/ClassifyCtx) plus an
interproc::CallGraph/scc_order/run_worklist fixed point that merges
capability evidence across call sites bidirectionally (callee behavior
taints caller argument; caller argument facts inform callee parameter),
via one symmetric PointerFact::merge_evidence_from applied per call site.

Deliberately scoped down, each gap filed as its own follow-up bead rather
than half-implemented: return-value aliasing not folded through calls yet
(slate-y0qs.4.3, returning a tracked pointer conservatively sets ESCAPE
for now); &mut str specialization skipped pending a UTF-8-safety fact
(slate-y0qs.4.4); Known-libc-function semantics only cover
free/memcpy/memmove/memset/str* family, everything else defaults to
ESCAPE (slate-y0qs.4.5).

solve() has no caller yet -- it's a read-only analysis
(BTreeMap<PointerBinding, PointerFact>). Wiring it into engine::apply()
and writing the two consuming NodeRules (decl-site lift +
.as_ptr()/.as_mut_ptr() insertion, then an accessor-deletion cleanup rule)
is slate-y0qs.4.2, blocked on this task, intentionally left for next
session.

Verified: cargo check --lib clean (only expected dead-code warnings since
nothing calls solve() yet), cargo clippy --lib no new warnings, cargo fmt
clean. No differential fixture yet since there's no rewrite to test --
slate-y0qs.4.2's acceptance criteria requires one.

Wrote up the full design as wiki/concepts/pointer-capability-lattice.md,
linked from wiki/index.md; rewrite-worklist-engine.md's original pointer
lattice section now points to it as superseded-but-kept-for-history.
