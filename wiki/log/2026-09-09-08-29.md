# Linux cpio dirent dlfcn header matrices

_2026-09-09 08:29:59_

Added cpio.h, dirent.h, and dlfcn.h to every Linux glibc/musl declaration manifest and generated their oracle-derived probes for x86_64, i386, ARM, and AArch64. Reconciled cpio constants as macros, glibc's GNU getdents64/MAXNAMLEN dirent surface, and glibc's Lmid_t/dlfcn macro surface while preserving musl ownership. crypt.h is present only in the configured x86_64/i386 glibc oracles (and all musl oracles); its libxcrypt macros now match the available profiles. elf.h remains unadmitted: glibc requires 500+ additional public macros, including cross-architecture ELF relocation namespaces, and needs a target-aware reconciliation rather than a partial header. Focused all-descriptor declaration matrices and the full release libc profile pass; cargo fmt --check and cargo clippy --all-targets -- -D warnings pass.
