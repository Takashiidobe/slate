# Verify five foundational Linux libc headers

_2026-09-09 08:03:54_

Added sys/types.h, string.h, stdlib.h, fcntl.h, and unistd.h to all eight glibc/musl declaration manifests and regenerated their oracle-derived probes. Removed stale transitive declarations from older glibc-x86_64 fixtures. Matched musl sys/types header exposure and wait-status macros, glibc C23 stdlib declarations, glibc TEMP_FAILURE_RETRY, and libc-specific string allocation helpers. Replaced Linux fcntl enum constants with target-owned macros, corrected openat2 constness, and added word-size, architecture, libc, and glibc-version gates for compatibility constants. Preserved existing argz.h assertions after regeneration. The focused matrix, full libc profile, rustfmt, and clippy pass.
